# Brooks Trading Coach - Environment Variables
# Copy this file to .env and fill in your values

# ==================== LLM CONFIGURATION ====================
# LLM Configuration (via LiteLLM proxy)
LLM_API_KEY=your-llm-api-key
LLM_BASE_URL=https://your-litellm-proxy.com/v1
LLM_MODEL=anthropic/claude-sonnet-4.5

# ==================== DATA PROVIDERS ====================
# Options: yfinance (free, slow), polygon (fast, requires key)
DATA_PROVIDER=yfinance

# Polygon.io API Key (recommended - faster and more reliable)
# Get free key at: https://polygon.io/
POLYGON_API_KEY=your-polygon-api-key

# Alpaca (optional)
ALPACA_API_KEY=your-alpaca-api-key
ALPACA_SECRET_KEY=your-alpaca-secret-key

# Databento (for futures, optional)
DATABENTO_API_KEY=your-databento-api-key

# Robinhood (credentials entered in web UI, session stored locally)
# No API key needed - uses username/password login

# ==================== APPLICATION SETTINGS ====================
LOG_LEVEL=INFO
OUTPUTS_DIR=outputs
CACHE_DIR=data/cache

# Risk Controls (soft guardrails)
MAX_DAILY_LOSS_R=3.0
MAX_LOSING_STREAK=3
DEFAULT_RISK_PER_TRADE_PCT=1.0

# ==================== SUPABASE (PRODUCTION) ====================
# Get these from your Supabase project: Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key

# ==================== DATABASE ====================
# For LOCAL DEVELOPMENT: Use SQLite (simpler, no setup needed)
DATABASE_URL=sqlite:///data/trades.db

# For PRODUCTION (Supabase PostgreSQL):
# Get from: Supabase Dashboard -> Settings -> Database -> Connection String -> URI
# Use "Transaction" mode (port 6543) for connection pooling
#
# Password must be URL-encoded (replace special chars):
#   @ -> %40, : -> %3A, / -> %2F, % -> %25, * -> %2A, ! -> %21, $ -> %24
#
# Example:
# DATABASE_URL=postgresql://postgres.PROJECT:URL_ENCODED_PASSWORD@aws-0-us-east-1.pooler.supabase.co:6543/postgres
